# Ponto de entrada: instancia a Simulation e roda. Vai virar servidor OPC-UA
# mais pra frente — por enquanto, zerado, sem gRPC/dashboard/controllers
# (isso tudo estava desenhado pra uma arquitetura que não existe mais).

[package]
name = "te_service"
version = "0.1.0"
edition = "2021"

[dependencies]
te_core = { path = "../tep" }
simulation_framework = { path = "../../simulation-framework" }
